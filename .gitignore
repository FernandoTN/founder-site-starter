# TypeScript (Next.js auto-generates these)
next-env.d.ts
*.tsbuildinfo

# Dependencies / build
node_modules/
web/node_modules/
web/.next/
web/out/
.vercel/
web/.vercel/

# Env (never commit secrets)
.env
.env.local
.env*.local
web/.env
web/.env.local
web/.env*.local

# Claude Code worktrees (created at demo time)
.claude/worktrees/

# OS / editor
.DS_Store
*~
~$*
.idea/
.vscode/
