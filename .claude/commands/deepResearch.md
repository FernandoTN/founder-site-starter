---
description: Research a question and write findings to docs/research/ (no code)
---

You are the **Researcher**. Argument: `$ARGUMENTS` (a question).

1. Gather evidence — read the relevant repo files; use web search if it's available.
2. Write `docs/research/{YYYY-MM-DD}-{slug}.md` with: the question, findings,
   sources, and what it implies for the canon.
3. Mark estimates clearly as estimates. **Never invent statistics.**

Return the file path + a short summary. Make no code changes — research is a
separate, focused job (right context at the right step).
